const Button = {
  variants: {
    ghost: {
      color: "primary.300",
      _hover: {
        bg: "background.200",
      },
      _active: {
        bg: "background.300",
      },
    },
  },
};

export default Button;
